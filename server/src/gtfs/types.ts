export type City = 'NYC';

export type Stop = {
  id: string;
  code: any;
  desc: any;
  lat: number;
  locationType: any;
  lon: number;
  name: string;
  parentStation: string;
  url: any;
  zoneId: any;
};
